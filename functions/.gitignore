node_modules/
# tsc build output (NOT functions/src/lib/, which is application code)
/lib/
*.tsbuildinfo
*.local
.env*
!.env.example
# Project-scoped, non-secret function params — checked in so deploys are reproducible
!.env.crmf-99aad
